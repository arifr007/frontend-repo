/**
 * Represents a user
 */
export interface User {
  // Unique identifier for the user
  id: string; 

  // Full name of the user.
  name: string; 

  // Email address of the user.
  email: string; 

  // address of the user.
  address: string; 
}