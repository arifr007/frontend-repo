import UserList from '@/components/UserList'

/**
 * Main application page that renders the UserList component.
 */
const AppPage = () => (
  <UserList /> // Simply return the UserList component
)

export default AppPage